server:
  port: 8080
  servlet:
    context-path: /premierleague
spring:
  datasource:
    url: "jdbc:mysql://192.168.223.29:3306/premierleague?autoReconnect=true"
    username: root
    password: 2162004
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  management:
    endpoints:
      web:
        exposure:
          include: health,info
    endpoint:
      health:
        show-details: always